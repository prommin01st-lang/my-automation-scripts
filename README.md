# DevContext Nexus - PowerShell CLI Tools

ชุดเครื่องมือ Command Line (PowerShell) สำหรับจัดการ Context Files บน DevContext Nexus API V2 ถูกออกแบบมาให้ใช้งานง่าย, ปลอดภัย, และมีประสิทธิภาพ

## ✨ คุณสมบัติเด่น

- **ใช้งานง่ายผ่าน Alias:** มาพร้อมกับ `nexus-profile.ps1` เพื่อสร้างคำสั่งสั้นๆ (เช่น `nx-upload-file`) ให้ใช้งานได้จากทุกที่ใน PowerShell
- **โหมด Interactive:** สคริปต์ส่วนใหญ่เป็นแบบโต้ตอบ จะแสดงรายการโปรเจกต์/ไฟล์ที่มีอยู่ให้เลือก และจะถามข้อมูลที่จำเป็นหากไม่ได้ระบุไว้เป็นพารามิเตอร์
- **ปลอดภัย:** มีระบบป้องกันการลบข้อมูลโดยการถามยืนยัน และมีการตรวจสอบนามสกุลไฟล์อันตรายก่อนอัปโหลด
- **ทนทานต่อความผิดพลาด:** หากใส่ชื่อโปรเจกต์หรือไฟล์ผิด สคริปต์จะแจ้งเตือนและให้ลองใหม่อีกครั้ง ไม่หยุดทำงานกลางคัน
- **จัดการ API Key อย่างปลอดภัย:** อ่านค่า API Key จาก Environment Variable เพื่อไม่ให้คีย์ปะปนอยู่ในโค้ดหรือประวัติคำสั่ง

---

## ⚙️ ข้อกำหนดเบื้องต้น

1.  **PowerShell:** สคริปต์ถูกพัฒนาและทดสอบบน PowerShell 7+ (แนะนำ) แต่อาจทำงานบน Windows PowerShell 5.1 ได้
2.  **API Key:** คุณต้องมี API Key ของ DevContext Nexus V2

---

## 🚀 การติดตั้งและตั้งค่า

### 1. ตั้งค่า API Key

สคริปต์จะอ่าน API Key จาก Environment Variable ที่ชื่อว่า `CONTEXT_NEXUS_API_KEY`

**วิธีตั้งค่า (แนะนำให้ตั้งค่าแบบถาวร):**

```powershell
# คำสั่งนี้จะตั้งค่า Environment Variable แบบถาวรสำหรับ User ปัจจุบัน
[System.Environment]::SetEnvironmentVariable('CONTEXT_NEXUS_API_KEY', 'YOUR_API_KEY_HERE', 'User')

# หลังจากรันคำสั่งแล้ว ให้ปิดและเปิดหน้าต่าง PowerShell ใหม่เพื่อให้การตั้งค่ามีผล
```

### 2. ติดตั้งคำสั่ง (มี 2 วิธี)

#### วิธีที่ 1: (แนะนำ) ใช้งานผ่าน Profile (`nexus-profile.ps1`)

วิธีนี้จะทำให้คุณสามารถใช้คำสั่งสั้นๆ เช่น `nx-list-projects` ได้จากทุกที่

1.  **เปิดไฟล์ Profile ของ PowerShell:**
    ```powershell
    code $PROFILE
    # หรือใช้ notepad $PROFILE
    ```

2.  **เพิ่มโค้ดด้านล่างนี้ลงในไฟล์ Profile:**
    (กรุณาเปลี่ยน `C:\path\to\your\scripts` เป็น Path จริงที่เก็บสคริปต์ของคุณ)
    ```powershell
    # โหลดสคริปต์ DevContext Nexus
    . "C:\path\to\your\scripts\ScriptV2\nexus-profile.ps1"
    ```
    **สำคัญ:** อย่าลืม **จุด** และ **เว้นวรรค** (`. `) ที่ด้านหน้า Path นะครับ

    **หมายเหตุสำคัญสำหรับนักพัฒนา:**
    ไฟล์ `nexus-profile.ps1` ที่คุณมีในปัจจุบันอาจจะชี้ไปยังสคริปต์เวอร์ชันเก่าบน GitHub หากคุณต้องการใช้งานสคริปต์ `*-v2.ps1` ล่าสุดที่มีอยู่ในเครื่อง หรือต้องการให้ `nexus-profile.ps1` ของคุณชี้ไปที่สคริปต์เวอร์ชัน `v2` ที่เป็นต้นฉบับของคุณ คุณจะต้องแก้ไขไฟล์ `nexus-profile.ps1` ดังนี้:

    *   **สำหรับใช้สคริปต์ `v2` ที่เป็นต้นฉบับของคุณจากรีโมท:** คุณจะต้องอัปเดตไฟล์ `nexus-profile.ps1` บน GitHub ของคุณให้เรียกใช้ไฟล์ `*-v2.ps1` แทน `*.ps1`
        **ตัวอย่าง:** เปลี่ยน `$scriptUrl = "$repoBase/upload-file.ps1"` เป็น `$scriptUrl = "$repoBase/upload-file-v2.ps1"` สำหรับทุกฟังก์ชัน

    *   **สำหรับใช้สคริปต์ `v2` ที่อยู่ในเครื่องของคุณสำหรับการพัฒนา:** ให้แก้ไขตัวแปร `$repoBase` ในไฟล์ `nexus-profile.ps1` ของคุณให้ชี้มาที่โฟลเดอร์ในเครื่องของคุณ (และแก้ไข `*.ps1` เป็น `*-v2.ps1` ใน `nexus-profile.ps1` ด้วย)
        **ตัวอย่าง:**
        ```powershell
        # $repoBase = "https://raw.githubusercontent.com/"
        $repoBase = "file:///F:/GitHubProject/Dev Context Nexus/Backend/scripts/ScriptV2"
        # และในฟังก์ชันต่างๆ เช่น
        # $scriptUrl = "$repoBase/upload-file-v2.ps1"
        ```

#### วิธีที่ 2: รันสคริปต์โดยตรง

หากคุณไม่ต้องการตั้งค่า Profile คุณสามารถรันไฟล์ `.ps1` ได้โดยตรงจากในโฟลเดอร์ `ScriptV2`

```powershell
# ตัวอย่างการรันสคริปต์โดยตรง
.\list-projects-v2.ps1
.\upload-file-v2.ps1 -ProjectName "MyWebApp" -LocalFile "C:\data\main.css"
```

---

## 📚 รายการคำสั่ง (Commands)

เมื่อติดตั้งผ่าน `nexus-profile.ps1` แล้ว (และปรับให้ชี้ไปที่ `v2` scripts แล้ว) จะสามารถใช้คำสั่งเหล่านี้ได้:

###  ภาพรวม (Overview)

| Command | Script | Description |
| :--- | :--- | :--- |
| `nx-overview` | `get-overview-v2.ps1` | แสดงภาพรวมของระบบทั้งหมด (จำนวนโปรเจกต์, จำนวนไฟล์) |
| `nx-list-projects` | `list-projects-v2.ps1` | แสดงรายชื่อโปรเจกต์ทั้งหมดที่มีในระบบ |
| `nx-list-files` | `list-files-v2.ps1` | แสดงรายชื่อไฟล์ทั้งหมดที่อยู่ในโปรเจกต์ที่กำหนด |

**ตัวอย่าง:**
```powershell
# ดูภาพรวม
nx-overview

# ดูรายชื่อโปรเจกต์
nx-list-projects

# ดูไฟล์ในโปรเจกต์ชื่อ "MyWebApp"
nx-list-files -ProjectName "MyWebApp"
```

### การจัดการโปรเจกต์ (Project Management)

| Command | Script | Description |
| :--- | :--- | :--- |
| `nx-create-project` | `create-project-v2.ps1` | สร้างโปรเจกต์ใหม่ |
| `nx-delete-project` | `delete-project-v2.ps1` | **(อันตราย)** ลบโปรเจกต์และไฟล์ทั้งหมดที่อยู่ข้างใน |

**ตัวอย่าง:**
```powershell
# สร้างโปรเจกต์ชื่อ "NewApp"
nx-create-project -ProjectName "NewApp"

# ลบโปรเจกต์ (สคริปต์จะถามเพื่อยืนยัน)
nx-delete-project -ProjectName "OldApp"
```

### การจัดการไฟล์ (File/Content Management)

| Command | Script | Description |
| :--- | :--- | :--- |
| `nx-upload-file` | `upload-file-v2.ps1` | อัปโหลดไฟล์จากเครื่องขึ้นไปยังโปรเจกต์ |
| `nx-upload-content` | `upload-content-v2.ps1` | อัปโหลดเนื้อหา (Text) โดยตรงเพื่อสร้างเป็นไฟล์ |
| `nx-download-context` | `download-context-v2.ps1` | ดาวน์โหลดไฟล์จากโปรเจกต์มาเก็บไว้ในเครื่อง |
| `nx-delete-content` | `delete-content-v2.ps1` | **(อันตราย)** ลบไฟล์ที่ระบุออกจากโปรเจกต์ |

**ตัวอย่าง:**
```powershell
# อัปโหลดไฟล์ main.js ไปยังโปรเจกต์ "MyWebApp" และตั้งชื่อไฟล์บน server เป็น "scripts/app.js"
nx-upload-file -ProjectName "MyWebApp" -LocalFile "C:\dev\main.js" -RemotePath "scripts/app.js"

# อัปโหลดเนื้อหา "Hello World" ไปสร้างเป็นไฟล์ "welcome.txt"
nx-upload-content -ProjectName "MyWebApp" -FilePath "welcome.txt" -Content "Hello World"

# ดาวน์โหลดไฟล์ "config/settings.json" มาไว้ที่ Desktop
nx-download-context -ProjectName "MyWebApp" -FilePath "config/settings.json" -SavePath "$HOME\Desktop\settings.json"

# ลบไฟล์ "old-styles.css"
nx-delete-content -ProjectName "MyWebApp" -FilePath "old-styles.css"
```
